<%# <div class="patterns-container">
    <div class="row"> 
        <%# <div class="col-lg-3 col-md-6 col-sm-12 pattern-box"> %>
<%# </div> %>
<%# </div> %>
<%# </div> %>
<div class="patterns-container">
  <% patterns.each do |pattern| %>
    <div class="pattern-box">   
        <% if pattern.complete or (user_signed_in? && current_user.id == pattern.user_id) %>
          <%= User.find(pattern.user_id).username %>
          <% if pattern.pictures.attached? %>
            <%= image_tag pattern.pictures[0].variant(resize_to_limit: [100, 100]) %>
          <% end %>
          <%= pattern.name %>
          <%= pattern.sizes %>
          <%= pattern.fabric %>
          <%= pattern.fabric_amount %>
          <%= Garment.find(pattern.garment_id).name %>
          <%= pattern.category %>
          <%= number_to_currency(pattern.price) %>
          <%= pattern.description %>
          <%= pattern.difficulty %>
          <%= pattern.notions %>
          <%= pattern.complete %>
          <%= link_to 'Show', pattern %>
          <% if user_signed_in? && (current_user.id == pattern.user_id && current_user.is_seller || current_user.admin) %>
            <%= link_to 'Edit', edit_pattern_path(pattern) %>
            <% if !pattern.complete || !pattern.transactions.any? %>
              <%= link_to 'Destroy', pattern, method: :delete, data: { confirm: 'Are you sure?' } %>
            <% end %>
          <% end %>
        <% end %>
    </div>
  <% end %>
</div>


